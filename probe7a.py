# this is the less than three script -- vll was here
from sys import argv

script, first, = argv

print("the script is called:", script)
print("We can make things up because:", first)
print(f"We can do anything with {script} because of {first}.")

